#ifndef __UNTIL_H__
#define __UNTIL_H__

#define test 1

int strpos(const char *haystack, const char *needle);
void ltrim(char *s);
void rtrim(char *s);
void trim(char *s);
int str2int(const char *s, size_t len, int *val);

#endif
